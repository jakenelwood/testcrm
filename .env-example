# Copy this file to .env and fill in the values
# For local development: cp .env-example .env
# For production: cp .env-example .env && nano .env

# General
SECRET_KEY=replace_with_your_secure_secret_key
SERVER_IP=65.21.174.252  # Replace with your server IP

# Database
DATABASE_URL=sqlite:///./quoterequest.db

# JWT Settings
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# CORS Settings
# Add any additional domains that need to access the API, separated by commas
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:80,http://localhost,http://65.21.174.252:3000

# LanceDB Settings
LANCEDB_URL=http://lancedb:8000

# API Settings
NEXT_PUBLIC_API_BASE_URL=http://65.21.174.252:8000  # Replace with your server IP 