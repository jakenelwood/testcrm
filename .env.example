# =============================================================================
# ENVIRONMENT VARIABLES TEMPLATE
# =============================================================================
# Copy this file to .env.local and fill in your actual values.
# NEVER commit .env.local or .env.production to version control!
#
# Security Guidelines:
# 1. Use different secrets for development and production
# 2. Generate strong secrets: openssl rand -base64 32
# 3. Never expose service role keys in client-side code
# 4. Rotate secrets regularly
# 5. Use environment-specific URLs and configurations

# =============================================================================
# SUPABASE CONFIGURATION
# =============================================================================
# Your Supabase project URL (found in Project Settings > API)
# Format: https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Supabase anonymous key (safe to expose in client-side code)
# This key allows read access to public tables and authenticated access for users
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here

# Supabase service role key (NEVER expose in client-side code)
# This key bypasses RLS and should only be used in server-side code
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# Supabase JWT secret (for token verification)
# Found in Project Settings > API > JWT Settings
SUPABASE_JWT_SECRET=your_jwt_secret_here

# =============================================================================
# AUTHENTICATION CONFIGURATION
# =============================================================================
# NextAuth secret (generate with: openssl rand -base64 32)
# CRITICAL: Use a different secret for each environment!
NEXTAUTH_SECRET=your_nextauth_secret_here

# NextAuth URL (your application URL)
# Development: http://localhost:3000
# Production: https://your-domain.com
NEXTAUTH_URL=http://localhost:3000

# =============================================================================
# OAUTH PROVIDERS
# =============================================================================
# Google OAuth Configuration
# Get these from Google Cloud Console > APIs & Services > Credentials
GOOGLE_OAUTH_CLIENT_ID=your_google_client_id_here
GOOGLE_OAUTH_CLIENT_SECRET=your_google_client_secret_here

# Microsoft/Azure OAuth Configuration
# Get these from Azure Portal > App Registrations
AZURE_OAUTH_CLIENT_ID=your_azure_client_id_here
AZURE_OAUTH_CLIENT_SECRET=your_azure_client_secret_here
# Optional: Azure tenant ID for single-tenant apps
# AZURE_TENANT_ID=your_azure_tenant_id_here

# =============================================================================
# RINGCENTRAL INTEGRATION
# =============================================================================
# RingCentral application credentials
# Get these from RingCentral Developer Console
RINGCENTRAL_CLIENT_ID=your_ringcentral_client_id
RINGCENTRAL_CLIENT_SECRET=your_ringcentral_client_secret

# RingCentral server URL
# Sandbox: https://platform.devtest.ringcentral.com
# Production: https://platform.ringcentral.com
RINGCENTRAL_SERVER_URL=https://platform.devtest.ringcentral.com

# RingCentral OAuth redirect URI
# Must match the URI configured in RingCentral app settings
RINGCENTRAL_REDIRECT_URI=http://localhost:3000/oauth-callback

# =============================================================================
# N8N INTEGRATION (OPTIONAL)
# =============================================================================
# n8n webhook URL for automation workflows
N8N_WEBHOOK_URL=http://your-n8n-instance:5678/webhook

# n8n API key for secure communication
N8N_API_KEY=your_n8n_api_key

# =============================================================================
# AI SERVICES (FUTURE INTEGRATION)
# =============================================================================
# OpenAI API key for AI features
# OPENAI_API_KEY=your_openai_key

# Anthropic API key for Claude integration
# ANTHROPIC_API_KEY=your_anthropic_key

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================
# Application environment (development, staging, production)
NODE_ENV=development

# Public application URL (used for redirects and API calls)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Database URL (if using direct database connections)
# Format: postgresql://postgres:[password]@db.your-project.supabase.co:5432/postgres
# DATABASE_URL=postgresql://postgres:password@localhost:5432/your_database

# =============================================================================
# SECURITY SETTINGS
# =============================================================================
# Enable/disable debug logging
DEBUG=false

# CORS allowed origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# =============================================================================
# MONITORING & ANALYTICS (OPTIONAL)
# =============================================================================
# Sentry DSN for error tracking
# SENTRY_DSN=your_sentry_dsn_here

# Google Analytics tracking ID
# NEXT_PUBLIC_GA_TRACKING_ID=your_ga_tracking_id

# PostHog API key for product analytics
# NEXT_PUBLIC_POSTHOG_KEY=your_posthog_key
# NEXT_PUBLIC_POSTHOG_HOST=https://app.posthog.com
